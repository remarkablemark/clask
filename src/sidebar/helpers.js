'use strict';

/**
 * Module dependencies.
 */
import { channels, directMessages } from '../../config/constants';

/** Constants. */
export const CHANNELS_TYPE = channels;
export const CREATE_CHANNEL_TYPE = 'createChannel';
export const DIRECT_MESSAGES_TYPE = directMessages;
