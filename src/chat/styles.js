'use strict';

const gutter = 24;

// styles
const styles = {
    gutter,
    buttonWidth: gutter * 2,
    formHeight: gutter * 4,
    messagePadding: gutter / 3,
    leftNavWidth: 224,
    leftNavMenuItemHeight: gutter * 1.5 + 'px'
};

module.exports = styles;
