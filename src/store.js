'use strict';

/**
 * Module dependencies.
 */
import { createStore } from 'redux';

/**
 * Reducer.
 */
function reducer(state, action) {
    return state;
}

/**
 * Export store.
 */
export default createStore(reducer);
